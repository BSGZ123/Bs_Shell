#!/usr/bin/env bash
#左括号是个特殊字符，需要\符号转义 ?说明(可以有也可以没有
# (| |-|\.) 必须匹配一个空格，一个连字符或一个点号
gawk --re-interval '/^\(?[2-9][0-9]{2}\)?(| |-|\.)[0-9]{3}( |-|\.)[0-9]{4}/{print $0}'
# sudo echo "317-555-1234" | sudo ./test2.sh
